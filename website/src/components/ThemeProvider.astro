---
---
<script is:inline>
    // This ensures that the theme is always dark on the website.
    // We don't support light mode because the Rive animation
    // doesn't look good in light mode since the background
    // is hardcoded to white.
    document.documentElement.dataset.theme = 'dark'
</script>